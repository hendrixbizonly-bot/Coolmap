# Copy to Server/.env.local and fill in. Never commit real values.
# Orca copies Server/.env.local from the main checkout into every new worktree (see .worktreeinclude).

# Vercel AI Gateway key used for Jev (typesafe-ai/jev). Also add it to the Vercel project env.
AI_GATEWAY_API_KEY=
